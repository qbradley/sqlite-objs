# Orchestration Log: Frodo (Agent-13) — Fix Shared Key Auth for Azurite

**Timestamp:** 2026-03-10T07:43:07Z  
**Agent:** Frodo (Azure Expert)  
**Mode:** background  
**Status:** IN PROGRESS

## Objective

Investigate and fix Shared Key authentication failure with Azurite when modifying existing blobs. Issue discovered during Layer 2 integration testing (Samwise Agent-12).

## Problem Statement

- **Symptom:** First PUT request succeeds; subsequent PUT requests fail with 403 Forbidden
- **Environment:** Azurite (local Azure emulator)
- **Impact:** Database creation works; modifications fail — blocks all meaningful testing
- **Blocker:** Cannot validate data persistence, transactions, or crash recovery

## Research Phase

### Questions to Answer
1. Does Azurite's Shared Key validator differ from Azure Storage REST API behavior?
2. Is the HMAC-SHA256 signature computed correctly for blob modification requests?
3. Does the Authorization header need changes for subsequent requests?
4. Is there an Azurite-specific configuration or authentication bypass?

### Expected Outcomes
- Root cause identified (Azurite bug vs code bug)
- Fix implemented and validated against Azurite
- Integration tests (Layer 2) pass with Shared Key auth
- Production path (real Azure) unblocked

## Dependencies
- **Blocks:** Layer 2 validation with Shared Key auth
- **Blocks:** Agent-13 cannot complete until root cause found
- **Awaits:** Samwise Layer 2 test results with SAS-token workaround

## Current Status
- Issue identified and documented
- Awaiting investigation to begin
- SAS-token workaround available for Layer 2 testing meanwhile

## Decision Context
- **D9 (Both SAS + Shared Key Auth):** Both methods required for MVP 1
- **D8 (Azure Error Mapping):** 403 should map to auth error (not retryable)
- **Frodo's Charter:** Azure expert — auth protocol investigation is primary domain
